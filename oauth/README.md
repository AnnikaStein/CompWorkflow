# OAuth Readme

Use this directory to store your application-specific parameters in a python script. Git is instructed to ignore any `.py` files in this directory for security reasons!

Locally on your machine, the depending scripts will look for a file called `myOAuthApplication.py`, it should contain what is described in this repo's main README. Hence, this directory only serves as a placeholder when you clone the repo!
